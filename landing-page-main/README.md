# Demo Frontend

Next.js app for the local settlement demo. The public app renders the demo console at `/` and exposes the Treasury execution adapter route at `/api/demo/treasury-orders`.

Run locally:

```powershell
npm ci
npm run dev -- -p 3000
```

Build:

```powershell
npm run build
```
